<Window x:Class="BIMOpsToolkit.Tools.Versioning.Views.VersionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BIMOpsToolkit.Tools.Versioning.Views"
             xmlns:viewmodel="clr-namespace:BIMOpsToolkit.Tools.Versioning.ViewModels"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             d:DataContext="{d:DesignInstance Type=viewmodel:VersionPanelViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400"
             Background="White"
             Title="{Binding SaveWindowTitle}"
             FontFamily="/BIMOpsToolkit.Tools;component/Resources/Roboto/Roboto-Regular.ttf#Roboto">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMOpsToolkit.Tools;component/UIComponents/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="70"/>
        </Grid.ColumnDefinitions>
        <Label Content="Name"
               Grid.Row="0"
               Grid.Column="0"/>
        <TextBox Text="123"
               Grid.Row="0"
               Grid.Column="1"
               Grid.ColumnSpan="3"
               Margin="5"
               Style="{DynamicResource RoundedTextBox}"
               IsEnabled="False"/>
        <Label Content="Note"
               Grid.Column="0"
               Grid.Row="1"/>
        <TextBox Grid.Column="1"
                 Grid.Row="1"
                 Grid.ColumnSpan="3"
                 Margin="5"
                 Style="{DynamicResource RoundedTextBox}"/>
        <Button Content="Cancel" 
                Style="{DynamicResource RoundedButton}"
                Grid.Column="2"
                Grid.Row="3"/>
        <Button Content="Save" 
                Style="{DynamicResource RoundedButton}"
                Grid.Column="3"
                Grid.Row="3"/>
    </Grid>
</Window>
